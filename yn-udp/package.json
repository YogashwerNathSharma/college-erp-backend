{
  "name": "yn-udp",
  "version": "1.0.0",
  "private": true,
  "description": "YN-UDP Visual Template Designer - Microservice for designing certificates, ID cards, report cards",
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install && cd server && npx prisma generate && npm run build",
    "start": "npm --prefix server run start",
    "install:all": "npm --prefix client install && npm --prefix server install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
