// FIX: remove invalid Student composite index referencing non-existent classId field.
// Student class membership is represented through Enrollment, while Student itself
// is scoped by tenantId + academicYearId.
